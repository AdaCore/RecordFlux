project Ping is

   for Languages use ("RecordFlux", "Python", "Ada");
   for Source_Dirs use (".", "specs", "unfinished", "generated");

   package Compiler is
      for Driver ("recordflux") use "";
   end Compiler;

   package Naming is
      for Spec_Suffix ("recordflux") use ".rflx";
   end Naming;

   package Recordflux is
      for Output_Dir use "generated";
   end Recordflux;

end Ping;
