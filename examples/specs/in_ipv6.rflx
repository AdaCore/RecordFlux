with IPv6;
with ICMPv6;
with UDP;
with IANA;

package In_IPv6 is

   for IPv6::Packet use (Data => UDP::Datagram)
      if Next_Header = IANA::PROT_UDP;

   for IPv6::Packet use (Data => ICMPv6::Message)
      if Next_Header = IANA::PROT_IPV6_ICMP;

end In_IPv6;
