with IPv6;
with ICMPv6;
with UDP;
with IANA;

package In_IPv6 is

   for IPv6::Packet use (Payload => UDP::Datagram)
      if Next_Header = IANA::Prot_UDP;

   for IPv6::Packet use (Payload => ICMPv6::Message)
      if Next_Header = IANA::Prot_IPv6_ICMP;

end In_IPv6;
